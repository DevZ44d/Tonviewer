# ─────────────────────────────────────────────
#  Tonviewer — Python Dependencies
#  https://github.com/DevZ44d/Tonviewer
# ─────────────────────────────────────────────

# ── Core (required) ───────────────────────────
requests>=2.31.0          # Sync HTTP client — wallet / tx / hash lookups
colorama>=0.4.6           # Terminal colour output for CLI
tonsdk>=1.0.14            # TON address normalisation (UQ format)

# ── Async & Scraping (NFT + Fragment modules) ─
httpx>=0.27.0             # Async HTTP client — NFTClient, FragmentClient
beautifulsoup4>=4.12.3    # HTML parser — Fragment.com scraper

# ── Build tools (dev / packaging only) ────────
setuptools>=61.0
wheel>=0.43.0
